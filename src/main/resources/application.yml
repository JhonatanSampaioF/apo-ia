spring:
  application:
    name: Apo.IA
  datasource:
    url: jdbc:mysql://apoia-server.mysql.database.azure.com.mysql.database.azure.com:3306/Apo-ia?useSSL=true&requireSSL=false&serverTimezone=UTC
    username: cfcgslijye@apoia-server.mysql.database.azure.com
    password: KiwiRosa@2025
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true

  ai:
    ollama:
      base-url: http://localhost:11434
      model: llama3
  thymeleaf:
    cache: false
  mvc:
    view:
      prefix: /templates/
      suffix: .html
  web:
    resources:
      static-locations: classpath:/static/
server:
  servlet:
    context-path: /


jwt:
  secret: ${JWT_SECRET:OhanaQuerDizerFamiliaFamiliaQuerDizerNuncaAbandonarOuEsquecer}
